[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecd-ml-project"
version = "0.1.0"
description = "KDHS 2022 KR — ECD Delay Prediction (36–59 months)"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
